import React from "react";

import HeaderLayout from "@components/HeaderLayout";

const AddEvent = () => {
  return (
    <HeaderLayout title="Add new event">
      <h1>Add events</h1>
    </HeaderLayout>
  );
};

export default AddEvent;
